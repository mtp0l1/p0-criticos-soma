<style>
  :root{
    /* neutros quentes */
    --ink:#221F1B; --ink-soft:#463F38; --muted:#736A61; --faint:#9C938A;
    --bg:#FBFAF7; --card:#FFFFFF; --line:#E7E1D8; --line-soft:#F1ECE4;

    /* acento editorial (terracota) */
    --accent:#A84E1D; --accent-ink:#8A3F16; --accent-soft:#FBF1E9; --accent-line:#E6CDB6;

    /* severidade */
    --red:#9E3423; --red-soft:#FAECE8; --red-line:#EBCCC4;
    --amber:#845309; --amber-soft:#FAF1E2; --amber-line:#E8D5B4;
    --slate:#5B6169; --slate-soft:#F1EFEB; --slate-line:#DDD7CE;
    --indigo:#464A87; --indigo-soft:#EDEDF5; --indigo-line:#CFD0E4;
    --teal:#155F57; --teal-soft:#E5F0EE; --teal-line:#B6D6D1;
    --green:#256C43; --green-soft:#EAF2EC; --green-line:#C3DCCC;

    --radius:12px; --radius-lg:16px;
    --shadow: 0 1px 1px rgba(34,31,27,.03), 0 6px 18px -14px rgba(34,31,27,.22);
    --shadow-lift: 0 2px 4px rgba(34,31,27,.04), 0 14px 30px -18px rgba(34,31,27,.28);
    --sans:'Instrument Sans', system-ui, -apple-system, 'Segoe UI', sans-serif;
    --serif:'Newsreader', Georgia, 'Times New Roman', serif;
    --pad-x: clamp(20px, 4vw, 64px);
    --pad-y: clamp(44px, 5vw, 76px);
  }
  *{box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  @media (prefers-reduced-motion: reduce){
    html{scroll-behavior:auto;}
    *{animation-duration:.01ms !important; transition-duration:.01ms !important;}
  }
  body{
    margin:0; font-family:var(--sans); background:var(--bg); color:var(--ink);
    line-height:1.62; font-size:16px; -webkit-font-smoothing:antialiased;
    overflow-x:hidden; text-wrap:pretty;
  }
  img{max-width:100%;}
  a{color:var(--accent-ink);}
  a:hover{color:var(--ink);}
  h1,h2,h3,h4,h5{margin:0;}
  :focus-visible{outline:2px solid var(--accent); outline-offset:3px; border-radius:4px;}
  ::selection{background:var(--accent-soft);}
  [id]{scroll-margin-top:88px;}

  .skip-link{
    position:fixed; left:16px; top:-60px; z-index:120; background:var(--ink); color:#fff;
    padding:10px 16px; border-radius:8px; font-size:14px; font-weight:600; text-decoration:none;
    transition:top .15s ease;
  }
  .skip-link:focus{top:16px; color:#fff;}

  /* progresso de leitura — visibilidade do status */
  .progress{position:fixed; inset:0 auto auto 0; height:2px; width:0; background:var(--accent); z-index:100;}

  /* ---------- Layout shell ---------- */
  .shell{display:flex; max-width:1520px; margin:0 auto; background:var(--card);}
  main{min-width:0; flex:1;}

  /* ---------- Sidebar ---------- */
  .sidebar{
    width:296px; flex-shrink:0; position:sticky; top:0; height:100vh; overflow-y:auto;
    border-right:1px solid var(--line); padding:26px 16px 56px; background:var(--bg);
    overscroll-behavior:contain;
  }
  .sidebar::-webkit-scrollbar{width:6px;}
  .sidebar::-webkit-scrollbar-thumb{background:var(--line); border-radius:6px;}
  .brand{display:flex; align-items:center; gap:11px; padding:0 8px 18px; margin-bottom:8px; border-bottom:1px solid var(--line);}
  .brand-mark{
    width:32px; height:32px; border-radius:9px; background:var(--ink); color:#fff; display:flex;
    align-items:center; justify-content:center; font-weight:600; font-size:13px; flex-shrink:0;
  }
  .brand-name{font-family:var(--serif); font-weight:600; font-size:17px; color:var(--ink); line-height:1.2;}
  .brand-sub{font-size:11px; color:var(--muted); letter-spacing:.06em; text-transform:uppercase; margin-top:2px;}

  .navgroup{margin-bottom:2px;}
  .navgroup-title{
    font-size:11px; font-weight:600; text-transform:uppercase; letter-spacing:.09em;
    color:var(--faint); padding:16px 8px 6px;
  }
  .navlink{
    display:flex; align-items:center; gap:10px; padding:9px 10px; border-radius:8px;
    font-size:14px; color:var(--ink-soft); text-decoration:none; font-weight:450;
    transition:background .15s ease, color .15s ease; min-height:38px;
  }
  .navlink:hover{background:var(--slate-soft); color:var(--ink);}
  .navlink.active{background:var(--accent-soft); color:var(--accent-ink); font-weight:600; box-shadow:inset 2px 0 0 var(--accent);}
  .navlink .n{
    width:22px; height:22px; border-radius:6px; background:var(--slate-soft); color:var(--muted);
    display:inline-flex; align-items:center; justify-content:center; font-size:10.5px; font-weight:600; flex-shrink:0;
  }
  .navlink.active .n{background:#fff; color:var(--accent-ink);}
  .navsub{display:flex; flex-direction:column; padding-left:26px; border-left:1px solid var(--line); margin-left:18px;}
  .navsub .navlink{font-size:13px; padding:7px 8px; min-height:34px; color:var(--muted);}
  .navsub .navlink:hover{color:var(--ink);}
  .toc-link{margin-bottom:6px; font-weight:600; color:var(--ink);}

  details.nav-scope{margin-bottom:4px;}
  details.nav-scope summary{
    cursor:pointer; list-style:none; display:flex; align-items:center; justify-content:space-between;
    padding:10px 8px; border-radius:8px; margin-bottom:2px; min-height:40px;
  }
  details.nav-scope summary .scope-chip{margin-top:0;}
  details.nav-scope summary::-webkit-details-marker{display:none;}
  details.nav-scope summary:hover{background:var(--slate-soft);}
  details.nav-scope summary::after{content:""; width:7px; height:7px; margin-left:8px; border-right:1.5px solid var(--faint); border-bottom:1.5px solid var(--faint); transform:rotate(45deg) translateY(-2px); transition:transform .18s ease;}
  details.nav-scope[open] summary::after{transform:rotate(225deg) translateY(-2px);}

  details.nav-item-details summary.navsub-toggle{
    cursor:pointer; list-style:none; display:inline-flex; align-items:center; gap:6px;
    padding:6px 8px; margin:0 0 2px 30px; font-size:11.5px; color:var(--muted); font-weight:500; user-select:none;
  }
  details.nav-item-details summary.navsub-toggle::-webkit-details-marker{display:none;}
  details.nav-item-details summary.navsub-toggle::after{content:""; width:5px; height:5px; border-right:1.5px solid currentColor; border-bottom:1.5px solid currentColor; transform:rotate(45deg) translateY(-1px); transition:transform .18s ease;}
  details.nav-item-details[open] summary.navsub-toggle::after{transform:rotate(225deg) translateY(-1px);}
  details.nav-item-details summary.navsub-toggle:hover{color:var(--ink);}

  /* ---------- Topbar (mobile) ---------- */
  .topbar{display:none;}
  .nav-backdrop{display:none;}

  /* ---------- Hero ---------- */
  .hero{
    padding:clamp(56px,7vw,104px) var(--pad-x) clamp(44px,5vw,72px);
    background:var(--ink); color:#F6F3EE; border-bottom:1px solid var(--ink);
  }
  .hero-kicker{
    font-size:12px; font-weight:600; letter-spacing:.16em; text-transform:uppercase;
    color:#E0B58C; margin-bottom:22px;
  }
  .hero h1{
    font-family:var(--serif); font-size:clamp(32px,4.4vw,54px); font-weight:400; line-height:1.1;
    letter-spacing:-0.015em; max-width:19em; color:#FDFBF8;
  }
  .hero p.lede{
    font-size:clamp(16px,1.4vw,18.5px); color:#C8C0B6; max-width:60ch; margin-top:22px; line-height:1.65;
  }
  .hero-actions{display:flex; gap:12px; flex-wrap:wrap; margin-top:32px;}
  .btn{
    display:inline-flex; align-items:center; gap:8px; min-height:46px; padding:12px 22px; border-radius:999px;
    font-size:14.5px; font-weight:600; text-decoration:none; transition:background .15s ease, border-color .15s ease, transform .15s ease;
  }
  .btn-primary{background:#F7F3ED; color:var(--ink);}
  .btn-primary:hover{background:#fff; color:var(--ink); transform:translateY(-1px);}
  .btn-ghost{border:1px solid rgba(247,243,237,.28); color:#EDE7DF;}
  .btn-ghost:hover{border-color:rgba(247,243,237,.6); color:#fff; background:rgba(255,255,255,.04);}
  .hero-meta{
    display:grid; grid-template-columns:repeat(auto-fit,minmax(190px,1fr)); gap:20px 28px; margin-top:40px;
    padding-top:28px; border-top:1px solid rgba(247,243,237,.14);
  }
  .hero-meta div{font-size:13px; color:#A79E94;}
  .hero-meta strong{display:block; color:#F6F3EE; font-size:15px; font-weight:600; margin-bottom:3px;}
  .hero-scope{
    display:inline-flex; align-items:center; gap:7px; padding:6px 14px; border-radius:999px;
    background:rgba(224,181,140,.12); border:1px solid rgba(224,181,140,.35); color:#E5BE99;
    font-size:11.5px; font-weight:600; text-transform:uppercase; letter-spacing:.07em; margin-bottom:18px;
  }

  /* ---------- Sections ---------- */
  section{padding:var(--pad-y) var(--pad-x); border-bottom:1px solid var(--line);}
  section:last-of-type{border-bottom:none;}
  .section-kicker{
    font-size:11.5px; font-weight:600; letter-spacing:.13em; text-transform:uppercase;
    color:var(--accent); margin-bottom:14px;
  }
  .section-title{
    font-family:var(--serif); font-size:clamp(26px,3vw,38px); font-weight:400; letter-spacing:-0.015em;
    color:var(--ink); margin-bottom:14px; line-height:1.15; max-width:24em;
  }
  .section-intro{font-size:16.5px; color:var(--ink-soft); max-width:66ch; margin-bottom:36px; line-height:1.7;}

  /* ---------- Stat grid ---------- */
  .stat-grid{display:grid; grid-template-columns:repeat(auto-fit,minmax(200px,1fr)); gap:14px; margin-bottom:36px;}
  .stat-card{background:var(--card); border:1px solid var(--line); border-radius:var(--radius); padding:22px 22px 20px;}
  .stat-num{font-family:var(--serif); font-size:40px; font-weight:400; line-height:1; color:var(--ink);}
  .stat-label{font-size:13px; color:var(--muted); margin-top:10px; line-height:1.45;}
  .stat-card.accent{background:var(--accent-soft); border-color:var(--accent-line);}
  .stat-card.accent .stat-num{color:var(--accent-ink);}
  .stat-card.red .stat-num{color:var(--red);}
  .stat-card.indigo .stat-num{color:var(--indigo);}

  .two-col{display:grid; grid-template-columns:1.25fr 1fr; gap:18px; margin-top:8px;}
  .panel{background:var(--card); border:1px solid var(--line); border-radius:var(--radius-lg); padding:26px 28px;}
  .panel h3{font-size:15px; font-weight:600; margin-bottom:14px; display:flex; align-items:center; gap:8px; color:var(--ink);}
  .panel ul{margin:0; padding-left:20px; display:flex; flex-direction:column; gap:10px;}
  .panel li{font-size:14.5px; color:var(--ink-soft); line-height:1.6;}
  .panel.risk{background:var(--red-soft); border-color:var(--red-line);}
  .panel.risk h3{color:var(--red);}
  .panel.risk p{font-size:14.5px; color:var(--ink-soft); margin:0; line-height:1.65;}

  /* ---------- flow chips ---------- */
  .flow-overview{display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:12px; margin-top:28px;}
  .flow-chip{
    display:flex; align-items:center; gap:12px; padding:14px 16px; background:var(--card);
    border:1px solid var(--line); border-radius:12px; text-decoration:none; min-height:64px;
    transition:border-color .15s, background .15s;
  }
  .flow-chip:hover{border-color:var(--accent-line); background:var(--accent-soft);}
  .flow-chip .ico{
    width:34px; height:34px; border-radius:9px; background:var(--slate-soft); display:flex; align-items:center;
    justify-content:center; font-weight:600; color:var(--muted); font-size:12px; flex-shrink:0;
  }
  .flow-chip .fc-title{font-size:14px; font-weight:600; color:var(--ink);}
  .flow-chip .fc-count{font-size:12px; color:var(--muted); margin-top:2px;}

  /* ---------- Badges ---------- */
  .badge{
    display:inline-flex; align-items:center; gap:6px; padding:4px 11px; border-radius:999px;
    font-size:11px; font-weight:600; letter-spacing:.04em; text-transform:uppercase; white-space:nowrap;
    border:1px solid transparent;
  }
  .badge.p0{background:var(--red-soft); color:var(--red); border-color:var(--red-line);}
  .badge.p1{background:var(--amber-soft); color:var(--amber); border-color:var(--amber-line);}
  .badge.p2{background:var(--slate-soft); color:var(--slate); border-color:var(--slate-line);}
  .badge.pattern{background:var(--indigo-soft); color:var(--indigo); border-color:var(--indigo-line);}
  .badge .dot{width:6px; height:6px; border-radius:50%; background:currentColor; flex-shrink:0;}

  /* ---------- Pattern cards ---------- */
  .pattern-card{
    background:var(--card); border:1px solid var(--line); border-radius:var(--radius-lg);
    padding:clamp(24px,3vw,36px); margin-bottom:18px; box-shadow:var(--shadow);
  }
  .pattern-head{display:flex; align-items:flex-start; justify-content:space-between; gap:20px; margin-bottom:14px; flex-wrap:wrap;}
  .pattern-num{font-size:11.5px; font-weight:600; color:var(--indigo); letter-spacing:.1em; margin-bottom:8px; text-transform:uppercase;}
  .pattern-title{font-family:var(--serif); font-size:clamp(20px,2vw,25px); font-weight:400; line-height:1.2; color:var(--ink); max-width:24em;}
  .pattern-body{display:grid; grid-template-columns:1fr 1fr; gap:26px; margin-top:20px;}
  .pattern-body h4{font-size:11px; font-weight:600; text-transform:uppercase; letter-spacing:.09em; color:var(--muted); margin-bottom:8px;}
  .pattern-body p{font-size:14.5px; color:var(--ink-soft); margin:0; line-height:1.65;}
  .occurrence-list{list-style:none; margin:0; padding:0; display:flex; flex-direction:column; gap:8px;}
  .occurrence-list li{font-size:14px; color:var(--ink-soft); padding-left:18px; position:relative; line-height:1.55;}
  .occurrence-list li::before{content:"—"; position:absolute; left:0; color:var(--faint);}
  .occurrence-list b{color:var(--ink); font-weight:600;}
  .pattern-footer{
    grid-column:1 / -1; margin-top:4px; padding-top:20px; border-top:1px solid var(--line-soft);
    display:flex; gap:28px; flex-wrap:wrap;
  }
  .pattern-footer .foot-block{flex:1; min-width:240px;}
  .pattern-footer h4{font-size:11px; font-weight:600; text-transform:uppercase; letter-spacing:.09em; color:var(--muted); margin-bottom:8px;}
  .pattern-footer p{font-size:14px; color:var(--ink-soft); margin:0; line-height:1.6;}

  /* ---------- Flow sections ---------- */
  .flow-section{padding:var(--pad-y) var(--pad-x); border-bottom:1px solid var(--line);}
  .flow-head{display:flex; align-items:center; gap:16px; margin-bottom:10px;}
  .flow-index{
    width:44px; height:44px; border-radius:12px; background:var(--ink); color:#fff;
    font-family:var(--serif); font-weight:400; font-size:19px;
    display:flex; align-items:center; justify-content:center; flex-shrink:0;
  }
  .flow-summary-row{display:flex; gap:8px; margin:20px 0 32px; flex-wrap:wrap; align-items:center;}

  .finding-card{
    border:1px solid var(--line); border-radius:var(--radius); padding:24px 26px; margin-bottom:12px;
    background:var(--card); box-shadow:var(--shadow);
  }
  .finding-card.p0{border-left:3px solid var(--red);}
  .finding-card.p1{border-left:3px solid var(--amber);}
  .finding-head{display:flex; align-items:flex-start; justify-content:space-between; gap:16px; margin-bottom:12px; flex-wrap:wrap;}
  .finding-title{font-size:17px; font-weight:600; color:var(--ink); max-width:44em; line-height:1.4; display:flex; align-items:center; flex-wrap:wrap; gap:4px;}
  .finding-grid{display:grid; grid-template-columns:repeat(3,1fr); gap:22px; margin-top:16px; padding-top:16px; border-top:1px solid var(--line-soft);}
  .finding-grid h5{font-size:10.5px; font-weight:600; text-transform:uppercase; letter-spacing:.09em; color:var(--muted); margin-bottom:7px;}
  .finding-grid p{font-size:14px; color:var(--ink-soft); margin:0; line-height:1.6;}
  .finding-grid .proposta{color:var(--ink);}
  .see-pattern{
    display:inline-flex; align-items:center; gap:6px; font-size:12.5px; font-weight:600; color:var(--indigo);
    text-decoration:none; margin-top:12px; min-height:32px; align-self:flex-start;
  }
  .see-pattern:hover{text-decoration:underline; color:var(--indigo);}

  .minor-block{margin-top:32px;}
  .minor-block h4{font-size:12.5px; font-weight:600; text-transform:uppercase; letter-spacing:.09em; color:var(--muted); margin-bottom:12px;}
  .minor-list{display:flex; flex-direction:column; gap:8px;}
  .minor-item{
    display:flex; gap:14px; align-items:flex-start; background:var(--card); border:1px solid var(--line);
    border-radius:10px; padding:14px 18px;
  }
  .minor-item .badge{flex-shrink:0; margin-top:1px;}
  .minor-item p{font-size:14px; margin:0; color:var(--ink-soft); line-height:1.55;}
  .minor-item p strong{color:var(--ink); font-weight:600;}

  /* ---------- Tabelas ---------- */
  .table-wrap{
    overflow-x:auto; border:1px solid var(--line); border-radius:var(--radius); background:var(--card);
    margin-top:12px; -webkit-overflow-scrolling:touch;
  }
  .table-wrap:focus-visible{outline:2px solid var(--accent); outline-offset:2px;}
  .writing-table{width:100%; border-collapse:collapse; min-width:640px;}
  .writing-table th{
    text-align:left; font-size:10.5px; text-transform:uppercase; letter-spacing:.09em; color:var(--muted);
    padding:16px 18px 12px; border-bottom:1px solid var(--line); font-weight:600; background:var(--bg);
    position:sticky; top:0;
  }
  .writing-table td{padding:18px; border-bottom:1px solid var(--line-soft); font-size:14px; vertical-align:top; color:var(--ink-soft);}
  .writing-table tr:last-child td{border-bottom:none;}
  .writing-table .flow-tag{font-size:11px; font-weight:600; color:var(--muted); text-transform:uppercase; letter-spacing:.05em;}
  .writing-table .old-text{color:var(--red);}
  .writing-table .new-text{color:var(--ink); font-weight:600;}
  .writing-table tbody tr:hover{background:var(--bg);}

  /* ---------- Footer ---------- */
  .footer{
    padding:48px var(--pad-x) 64px; color:var(--muted); font-size:13.5px; text-align:center;
    border-top:1px solid var(--line); background:var(--bg);
  }

  /* ---------- misc ---------- */
  .kicker-inline{font-size:11px; font-weight:600; letter-spacing:.1em; text-transform:uppercase; color:var(--muted); margin-bottom:8px;}
  .legend{display:flex; gap:20px; flex-wrap:wrap; margin-top:8px;}
  .legend-item{display:flex; align-items:center; gap:8px; font-size:13px; color:var(--muted);}
  .legend-dot{width:9px; height:9px; border-radius:50%; flex-shrink:0;}

  /* ---------- Evidências ---------- */
  .evidence{margin-top:18px; border-top:1px solid var(--line-soft); padding-top:18px;}
  .evidence-label{font-size:10.5px; font-weight:600; text-transform:uppercase; letter-spacing:.09em; color:var(--muted); margin-bottom:12px;}
  .evidence-row{display:flex; gap:16px; flex-wrap:wrap; align-items:flex-start;}
  .evidence-shot{
    max-width:220px; margin:0; border-radius:10px; border:1px solid var(--line); overflow:hidden;
    background:var(--bg);
  }
  .evidence-shot img{display:block; width:100%; height:auto;}
  .evidence-cap{font-size:12px; color:var(--muted); padding:10px 12px 12px; text-align:left; line-height:1.45;}
  .evidence-arrow{font-size:18px; color:var(--faint); align-self:center; flex-shrink:0;}

  .pattern-mention{
    border:1px solid var(--indigo-line); background:var(--indigo-soft); border-radius:12px;
    padding:18px 20px; margin-top:16px;
  }
  .pattern-mention .see-pattern{margin-top:0;}
  .pattern-mention .evidence{border-top:1px solid var(--indigo-line); margin-top:14px; padding-top:14px;}
  .pattern-mention .evidence-label{color:var(--indigo);}
  .pattern-mention .evidence-shot{background:#fff;}

  /* ---------- Escopo ---------- */
  .scope-chip{
    display:inline-flex; align-items:center; gap:6px; margin-top:10px; padding:5px 11px; border-radius:999px;
    background:var(--accent-soft); color:var(--accent-ink); font-size:10.5px; font-weight:600;
    text-transform:uppercase; letter-spacing:.06em;
  }
  .scope-panel{
    background:var(--accent-soft); border:1px solid var(--accent-line); border-radius:var(--radius-lg);
    padding:24px 28px; margin:0 0 34px;
  }
  .scope-panel h3{font-size:11.5px; font-weight:600; text-transform:uppercase; letter-spacing:.09em; color:var(--accent-ink); margin-bottom:12px;}
  .scope-panel p{font-size:15px; color:var(--ink-soft); margin:0; line-height:1.7;}
  .scope-panel p + p{margin-top:12px;}

  .scope-chip.shared{background:var(--indigo-soft); color:var(--indigo);}
  .scope-chip.mini{margin-top:0; padding:3px 9px; font-size:9.5px;}
  .scope-badge{
    display:inline-flex; align-items:center; gap:7px; padding:6px 13px; border-radius:999px;
    background:var(--indigo-soft); border:1px solid var(--indigo-line); color:var(--indigo);
    font-size:11px; font-weight:600; text-transform:uppercase; letter-spacing:.06em; margin-bottom:16px;
  }
  .scope-panel.shared{background:var(--indigo-soft); border-color:var(--indigo-line);}
  .scope-panel.shared h3{color:var(--indigo);}

  .scope-chip.existing{background:var(--teal-soft); color:var(--teal);}
  .scope-badge.existing{background:var(--teal-soft); border-color:var(--teal-line); color:var(--teal);}
  .scope-panel.existing{background:var(--teal-soft); border-color:var(--teal-line);}
  .scope-panel.existing h3{color:var(--teal);}
  .cross-tag{
    display:inline-flex; align-items:center; gap:5px; padding:3px 9px; border-radius:999px;
    background:var(--teal-soft); color:var(--teal); font-size:10px; font-weight:600; text-transform:uppercase;
    letter-spacing:.05em; margin-left:8px; vertical-align:middle; white-space:nowrap;
  }

  .ref-callout{
    background:var(--green-soft); border:1px solid var(--green-line); border-radius:12px;
    padding:20px 22px; margin-top:18px;
  }
  .ref-callout h4{font-size:11px; font-weight:600; text-transform:uppercase; letter-spacing:.09em; color:var(--green); margin-bottom:8px;}
  .ref-callout p{font-size:14.5px; color:var(--ink-soft); margin:0; line-height:1.65;}

  /* ---------- Roadmap ---------- */
  .roadmap-list{display:flex; flex-direction:column; gap:8px; margin-top:8px;}
  .roadmap-item{
    display:flex; align-items:flex-start; gap:16px; background:var(--card); border:1px solid var(--line);
    border-radius:10px; padding:16px 20px;
  }
  .roadmap-item .rn{
    width:28px; height:28px; border-radius:8px; background:var(--slate-soft); color:var(--ink);
    font-weight:600; font-size:12.5px; display:flex; align-items:center; justify-content:center;
    flex-shrink:0; margin-top:1px;
  }
  .roadmap-item .badge{flex-shrink:0; margin-top:2px;}
  .roadmap-item p{margin:0; font-size:14.5px; color:var(--ink-soft); line-height:1.6; padding-top:3px;}

  /* ---------- Sitemap cards ---------- */
  .map-grid{display:grid; grid-template-columns:repeat(auto-fit,minmax(280px,1fr)); gap:16px; margin:28px 0 36px;}
  .map-card{
    background:var(--card); border:1px solid var(--line); border-radius:var(--radius-lg);
    padding:26px 24px; box-shadow:var(--shadow); text-decoration:none; display:flex; flex-direction:column;
    transition:box-shadow .18s ease, border-color .18s ease, transform .18s ease;
  }
  .map-card:hover{transform:translateY(-2px); border-color:var(--accent-line); box-shadow:var(--shadow-lift);}
  .map-card .map-chip{
    display:inline-flex; align-self:flex-start; font-size:10px; font-weight:600; text-transform:uppercase;
    letter-spacing:.09em; color:var(--accent-ink); background:var(--accent-soft); padding:4px 10px;
    border-radius:999px; margin-bottom:14px;
  }
  .map-card.shared .map-chip{color:var(--indigo); background:var(--indigo-soft);}
  .map-card.existing .map-chip{color:var(--teal); background:var(--teal-soft);}
  .map-card h3{font-family:var(--serif); font-size:22px; font-weight:400; color:var(--ink); margin-bottom:8px; line-height:1.2;}
  .map-card p{font-size:14px; color:var(--muted); margin:0 0 18px; line-height:1.55;}
  .map-card .map-stats{display:flex; gap:20px; flex-wrap:wrap; margin-top:auto; padding-top:16px; border-top:1px solid var(--line-soft);}
  .map-card .map-stat strong{display:block; font-family:var(--serif); font-size:22px; color:var(--ink); font-weight:400; line-height:1.2;}
  .map-card .map-stat span{font-size:11.5px; color:var(--muted);}
  .map-card .map-cta{margin-top:18px; font-size:13px; font-weight:600; color:var(--accent-ink);}
  .map-card.shared .map-cta{color:var(--indigo);}
  .map-card.existing .map-cta{color:var(--teal);}

  /* ---------- Progressive disclosure ---------- */
  .scope-panel > p + details.scope-detail{margin-top:14px;}
  details.scope-detail summary{
    cursor:pointer; list-style:none; display:inline-flex; align-items:center; gap:6px;
    font-size:13px; font-weight:600; color:var(--ink); user-select:none; min-height:34px;
  }
  details.scope-detail summary::-webkit-details-marker{display:none;}
  details.scope-detail summary::after{content:""; width:6px; height:6px; border-right:1.5px solid currentColor; border-bottom:1.5px solid currentColor; transform:rotate(45deg) translateY(-1px); transition:transform .18s ease;}
  details.scope-detail[open] summary::after{transform:rotate(225deg) translateY(-1px);}
  details.scope-detail summary:hover{text-decoration:underline;}
  .scope-detail-body{margin-top:12px; padding-top:14px; border-top:1px solid rgba(34,31,27,.12);}
  .scope-detail-body p{font-size:15px; color:var(--ink-soft); line-height:1.7; margin:0;}
  .scope-detail-body p + p{margin-top:12px;}

  /* ---------- Responsivo ---------- */
  @media (max-width: 1040px){
    .topbar{
      display:flex; align-items:center; justify-content:space-between; gap:12px;
      position:sticky; top:0; z-index:60; background:rgba(251,250,247,.94);
      backdrop-filter:saturate(140%) blur(8px); border-bottom:1px solid var(--line);
      padding:10px 16px;
    }
    .topbar .brand{border:none; margin:0; padding:0;}
    .nav-toggle{
      display:inline-flex; align-items:center; gap:8px; min-height:44px; padding:10px 16px;
      background:var(--card); border:1px solid var(--line); border-radius:999px; color:var(--ink);
      font-family:var(--sans); font-size:14px; font-weight:600; cursor:pointer;
    }
    .nav-toggle:hover{background:var(--slate-soft);}
    .shell{display:block;}
    .sidebar{
      position:fixed; inset:0 auto 0 0; z-index:80; width:min(88vw,320px); height:100dvh;
      transform:translateX(-102%); transition:transform .22s ease; box-shadow:var(--shadow-lift);
      padding-top:20px;
    }
    body.nav-open .sidebar{transform:translateX(0);}
    .nav-backdrop{
      display:block; position:fixed; inset:0; z-index:70; background:rgba(34,31,27,.4);
      opacity:0; pointer-events:none; transition:opacity .22s ease;
    }
    body.nav-open .nav-backdrop{opacity:1; pointer-events:auto;}
    body.nav-open{overflow:hidden;}
    [id]{scroll-margin-top:72px;}
  }
  @media (max-width: 760px){
    .two-col, .pattern-body, .finding-grid{grid-template-columns:1fr;}
    .finding-grid{gap:18px;}
    .stat-grid{grid-template-columns:repeat(2,minmax(0,1fr));}
    .evidence-shot{max-width:calc(50% - 8px);}
    .evidence-arrow{display:none;}
    .section-intro{font-size:15.5px;}
    .footer{text-align:left;}
  }
  @media (max-width: 420px){
    .stat-grid{grid-template-columns:1fr;}
    .evidence-shot{max-width:100%;}
  }

  @media print{
    .sidebar, .topbar, .nav-backdrop, .progress, .skip-link{display:none !important;}
    body{background:#fff; font-size:11pt;}
    .hero{background:#fff; color:#000;}
    .hero h1, .hero p.lede, .hero-meta strong, .hero-meta div{color:#000;}
    section, .flow-section{padding:24pt 0; break-inside:avoid;}
    .pattern-card, .finding-card, .map-card{box-shadow:none;}
  }
</style>
